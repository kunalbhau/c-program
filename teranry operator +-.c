#include<stdio.h>

int main()
{

    int num;
    printf("enter a  number");
    scanf("%d", &num);

    {
    num > 0?
    printf("it is positive") : printf(" it is negative");
    }
    return 0;
}
